get server version:
    select version();
    show variables like ‘%version%’

list users:
    select user();

create user:
    CREATE USER 'jeffrey'@'localhost' IDENTIFIED BY 'password';

set password
    SET PASSWORD FOR 'jeffrey'@'localhost' = PASSWORD('password');
    PASSWORD is a function,put real password string in this function
    


start stop restart
mac:
	cd /usr/local/mysql/support-files 
	mysql.server start
	mysql.server stop
	mysql.server  restart


alter table table_name auto_increment= 1;

mysql 条件插入

insert into person_table (uid,pname,age)
select 2,"李四",20 from dual
where not exists
(select id from person_table where uid=2 )



sql_mode=STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION

set @@GLOBAL.sql_mode='STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION'


select t_order.picker_phone as phone, t_order.time, t_ob_action.bonus
            , if(money.id is not null, 2, if(t_order.real_money = 0, 3, 1)) as status
            from t_ob_action join t_order on t_ob_action.order_id = t_order.id
                left join (select id, order_id from t_project_money) money on t_ob_action.order_id = money.order_id
            where t_ob_action.uid = #{uid} and t_ob_action.action = 2
            and t_order.time between #{timeStart, jdbcType=TIMESTAMP} and #{timeEnd, jdbcType=TIMESTAMP}
            order by t_ob_action.id desc limit #{index}, #{size};
